# 03. 문서 정의서 (Document Index)

제작에 필요한 전체 문서 목록. 각 문서의 목적, 작성 시점, 승인 조건을 정의한다.
문서 번호가 곧 파일명 접두어이며, 모두 `docs/` 아래에 둔다.

- 문서 버전: v0.1
- 작성일: 2026-07-09

## 문서 상태 정의

`초안` → `검토` → `승인` → (변경 시) `개정`. 승인된 문서의 변경은 문서 내 변경 이력 표에 기록한다.

## 문서 목록

### 기획 (Phase 0에서 확정)

| 문서 | 목적 | 상태 | 승인 조건 |
|---|---|---|---|
| `01-project-plan.md` 프로젝트 계획서 | 목표·아키텍처·기술선택·로드맵·리스크의 단일 출처 | **초안 작성됨** | 기술 선택안과 Phase 구분에 발주자 동의 |
| `02-functional-requirements.md` 기능 요구서 | 기능/비기능 요구사항과 수용 기준. 개발 범위의 기준선 | **초안 작성됨** | P0 목록과 Open Questions 답변 확정 |
| `03-document-index.md` 문서 정의서 | (본 문서) 문서 체계 정의 | **초안 작성됨** | — |

### 조사 (Phase 0 산출물 — 코드 착수 전 필수)

| 문서 | 목적 | 상태 | 승인 조건 |
|---|---|---|---|
| `10-collection-research.md` 플랫폼 수집 조사서 | 3사별 접근 경로(웹/내부API/**RPA**), robots.txt·약관 검토 결과, 지역 파라미터 구조(당근), 레이트리밋 정책 결정, **자동 채팅(FR-D6) 가능 경로와 계정 제재 리스크 평가**. **NFR-4에 따라 이 문서 승인 전 해당 플랫폼 수집 코드 머지 금지** | **초안 작성됨** (약관 원문 TODO 잔존) | 플랫폼별 판정 완료(됨) + 약관 원문 확인 + 발주자 승인 |
| `11-model-benchmark.md` AI 모델 벤치마크 보고서 | 벤치마크 시점 SOTA 경량 LLM 2~3종 후보 조사(후보 예: Qwythos-9B, 한국어 성능·NFR-1 구동 가능성 기준)와 본문 추출 품질, VL 경량 모델 사진 판별 실용성 측정. 실매물 텍스트 30건·사진 30장 기준 | 미착수 | Q3·Q4(기능 요구서) 답변 도출 |

### 설계 (Phase 1 착수 시)

| 문서 | 목적 | 승인 조건 |
|---|---|---|
| `20-architecture.md` 시스템 설계서 | 컴포넌트 경계, 어댑터 인터페이스 시그니처, 큐/스케줄링 흐름, 장애 격리 설계(NFR-6), 배포 구성 | 계획서 §3의 구체화, 인터페이스 확정 |
| `21-data-model.md` 데이터 모델 정의서 | Category/Product/Watch/Listing/AnalysisReport/MatchResult의 전체 필드·제약·인덱스, 파기 정책(NFR-5) | ERD와 마이그레이션 초안 포함 |
| `22-ontology-spec.md` 온톨로지 명세서 | 카테고리 트리·속성 스키마·별칭 사전의 YAML 포맷 정의와 초기 데이터(스마트폰·모니터·노트북), 별칭 갱신 프로세스(FR-B4) | YAML 스키마 검증기 동작 |
| `23-ai-pipeline-spec.md` AI 파이프라인 명세서 | 3단계 깔때기의 단계별 입출력 계약, LLM/VL 프롬프트 전문, 스키마 검증·재시도 정책, `언급없음` 처리 규칙, 프롬프트 버전 관리(NFR-7) | 프롬프트가 골든셋에서 목표 정확도 달성 |
| `24-scoring-spec.md` 매칭 스코어링 명세서 | 점수 공식, 필수/선호/언급없음/상충/신뢰도 가중치의 수치 정의, 예제 계산표 | FR-C5 단위 테스트가 본 문서 예제와 일치 |
| `25-notification-spec.md` 알림 명세서 | 웹 대시보드 매칭 표시·매칭 상세 포맷, 중복 방지 규칙, 피드백 버튼 (푸시 채널 없음) | 화면 스크린샷으로 검토 |
| `26-web-ui-spec.md` 웹 UI·인증 명세서 | 화면 목록(로그인/Watch 목록·수정/매물 이력/매칭 상세/시세/문의), 자체 인증 흐름(FR-A1), 세션 관리, HTTPS 배포 전제 | Watch 등록 E2E 시나리오 통과 |
| `33-autochat-acceptance.md` 자동 채팅 인수 절차 | 로그인 세션 저장·셀렉터 기입·`chat-check` 검증·dry-run·실발송 전환 체크리스트 (FR-D6) | **작성됨** |

### 품질·운영 (Phase 1~2)

| 문서 | 목적 | 승인 조건 |
|---|---|---|
| `12-ai-eval-plan.md` AI 평가 계획서 | 골든셋 구축 방법(라벨링 가이드 포함), 정확도 지표 정의, CI 회귀 평가 절차(NFR-8) | 골든셋 100건 라벨링 완료 |
| `30-test-plan.md` 테스트 계획서 | 단위/통합/E2E 범위, 어댑터 목킹 전략(실 플랫폼 호출 없는 테스트), 릴리스 기준 | CI 파이프라인 구성 완료 |
| `31-operations.md` 운영 가이드 | 설치·설정(모델 다운로드 포함), 헬스체크 대응 절차(FR-D4), 어댑터 장애 시 복구 플레이북, 백업 | Phase 2 완료 시점 |
| `32-privacy-compliance.md` 데이터·컴플라이언스 정책 | 수집 데이터 보관·파기, 개인정보 최소화 원칙, 플랫폼별 준수 사항 요약 (10번 문서의 운영 규칙화) | 법적 검토 사항 반영 |

## 작성 순서와 의존 관계

```
01 계획서 ─┬→ 10 수집 조사 ──────────┬→ 20 설계서 → 21 데이터 모델
           │→ 11 모델 벤치마크 ─┐     │
02 요구서 ─┘                   ├──→ 23 AI 파이프라인 ← 22 온톨로지
                               │         ↓
                        12 평가 계획   24 스코어링 → 25 알림 / 26 웹UI
                                          ↓
                               30 테스트 / 31 운영 / 32 컴플라이언스
```

**다음 액션**: ~~Q5 답변~~(해결) → ~~10번 조사 착수~~(초안 완료) → ① 10번 문서 발주자 검토: 자동 채팅 반자동 권고(§7) 수용 여부 + 약관 원문 TODO 마무리 → ② `11-model-benchmark.md` 착수 (SOTA 경량 모델 후보 조사부터).
